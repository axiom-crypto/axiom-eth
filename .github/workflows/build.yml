name: Build workspace

on:
  push:
    branches: ["main"]
  pull_request: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          export AXIOM_SKIP_CONSTANT_GEN=1
          cargo build --verbose
